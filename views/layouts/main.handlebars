<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Metadata -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Page Title -->
    <title>UniComm</title>
    <!-- Stylesheets -->
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Bootstrap CSS (External library) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <!-- Header Section -->
    <header>
        <!-- Navigation Menu -->
        <nav>
            <!-- Home Link -->
            <a href="/">Home</a>
            <!-- Conditional Links based on user authentication -->
            {{#if logged_in}}
            <!-- Dashboard Link (Visible if the user is logged in) -->
            <a href="/dashboard">Dashboard</a>
            <!-- Create a Post Link (Visible if the user is logged in) -->
            <a href="/dashboard/createpost">Create a Post</a>
            <!-- Logout Link (Visible if the user is logged in) -->
            <a id="logout" href="javascript:void(0)">Logout</a>
            {{else}}
            <!-- Login Link (Visible if the user is not logged in) -->
            <a href="/login">Login</a>
            <!-- Signup Link (Visible if the user is not logged in) -->
            <a href="/signup">Signup</a>
            {{/if}}
        </nav>
        <div class="d-flex justify-content-center">
            <div class="container">
                <div class="your-div mx-auto text-center" style="width: 50%;">
                    <h3 id="UniComm" href="javascript:void(0)">Welcome to UniComm</h3>
                </div>
            </div>
            </div>
    </header>

    <!-- Main Content (This is where the main content of the page will be inserted) -->
    {{{ body }}}

    <!-- JavaScript Files -->
    <!-- Custom JavaScript File -->
    <script src="/js/script.js"></script>
    <!-- JavaScript for Logout Functionality -->
    <script src="/js/logout.js"></script>
</body>

</html>